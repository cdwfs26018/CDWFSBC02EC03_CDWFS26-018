{% extends 'base.html.twig' %}

{% block body %}
    <h2>Donner un avis pour {{ evenement.titre }}</h2>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">Envoyer</button>
    {{ form_end(form) }}
{% endblock %}
